# Структурные паттерны проектирования (Structural Design Patterns)

Этот репозиторий содержит примеры реализации всех основных структурных паттернов проектирования на Python.

## Что такое структурные паттерны?

Структурные паттерны определяют различные сложные структуры, которые изменяют интерфейс уже существующих объектов или его реализацию, позволяя облегчить разработку и оптимизировать программу.

## Реализованные паттерны

### 1. Adapter (Адаптер) 🔌
**Файл:** `adapter.py`

Позволяет объектам с несовместимыми интерфейсами работать вместе. Преобразует интерфейс одного класса в интерфейс другого, ожидаемый клиентами.

**Пример:** Адаптер для подключения европейских устройств к американским розеткам.

### 2. Bridge (Мост) 🌉
**Файл:** `bridge.py`

Отделяет абстракцию от её реализации так, чтобы и то, и другое можно было изменять независимо.

**Пример:** Рисование различных фигур разными цветами, где фигуры и цвета могут варьироваться независимо.

### 3. Composite (Компоновщик) 🌳
**Файл:** `composite.py`

Объединяет объекты в древовидную структуру для представления иерархии. Позволяет клиентам единообразно трактовать индивидуальные и составные объекты.

**Пример:** Файловая система с файлами и директориями.

### 4. Decorator (Декоратор) 🎨
**Файл:** `decorator.py`

Динамически добавляет объектам новые обязанности. Является гибкой альтернативой порождению подклассов для расширения функциональности.

**Пример:** Добавление различных ингредиентов к кофе (молоко, сахар, взбитые сливки).

### 5. Facade (Фасад) 🏛️
**Файл:** `facade.py`

Предоставляет унифицированный интерфейс к набору интерфейсов в подсистеме. Определяет высокоуровневый интерфейс, упрощающий использование подсистемы.

**Пример:** Упрощенный интерфейс для запуска компьютера, скрывающий сложность работы процессора, памяти и жесткого диска.

### 6. Flyweight (Приспособленец) 🪶
**Файл:** `flyweight.py`

Использует разделение для эффективной поддержки множества мелких объектов. Экономит память за счет разделения состояния между похожими объектами.

**Пример:** Лес с множеством деревьев, где типы деревьев (дуб, сосна, береза) разделяются между экземплярами.

### 7. Proxy (Заместитель) 🛡️
**Файл:** `proxy.py`

Предоставляет суррогат или заместитель другого объекта для контроля доступа к нему. Может добавлять дополнительную функциональность при обращении к объекту.

**Пример:** Ленивая загрузка изображений и контроль доступа к защищенным ресурсам.

## Запуск примеров

### Запустить все паттерны сразу:
```bash
python main.py
```

### Запустить отдельный паттерн:
```bash
python adapter.py
python bridge.py
python composite.py
python decorator.py
python facade.py
python flyweight.py
python proxy.py
```

## Требования

- Python 3.6 или выше

## Структура проекта

```
StructuralPattern/
├── README.md          # Документация
├── main.py           # Запуск всех демонстраций
├── adapter.py        # Паттерн Адаптер
├── bridge.py         # Паттерн Мост
├── composite.py      # Паттерн Компоновщик
├── decorator.py      # Паттерн Декоратор
├── facade.py         # Паттерн Фасад
├── flyweight.py      # Паттерн Приспособленец
└── proxy.py          # Паттерн Заместитель
```

## Преимущества структурных паттернов

- **Гибкость:** Легко изменять и расширять код
- **Переиспользование:** Код можно использовать повторно
- **Упрощение:** Сложные структуры становятся понятнее
- **Оптимизация:** Эффективное использование памяти и ресурсов

## Дополнительная информация

Структурные паттерны проектирования являются частью классической книги "Design Patterns: Elements of Reusable Object-Oriented Software" (Gang of Four, GoF).

## Автор

Nikita Shchegolev